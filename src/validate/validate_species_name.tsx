	
export const validateSpeciesName : (value : string) => Array<string> = (value) => {
	// validation code here
	// needs to return an array of error messages
	// or an empty array if there aren't any
	return [];
};